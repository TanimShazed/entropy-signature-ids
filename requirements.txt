# Core dependencies for notebook/entropy_signature_ids.ipynb
# The notebook installs these itself on first run; this file is provided for
# local/offline setups (see "Reproducing the results" in README.md).

numpy>=2.0
pandas>=2.0
scikit-learn>=1.3
scipy>=1.10
matplotlib>=3.7

# Optional: only used for GPU-availability logging (informational), not required
# by the method itself, which is CPU-only by design. Safe to omit.
torch>=2.0

# Required only when running inside Google Colab (triggers the results.zip
# download at the end of the notebook). Not installable outside Colab; the
# notebook catches the ImportError and skips this step automatically.
# google-colab
